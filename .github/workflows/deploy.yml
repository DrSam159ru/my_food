name: Deploy Foodgram to Production

on:
  push:
    branches: [ "main" ]

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push backend
        uses: docker/build-push-action@v6
        with:
          context: ./backend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/foodgram-backend:latest

      - name: Build and push frontend
        uses: docker/build-push-action@v6
        with:
          context: ./frontend
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/foodgram-frontend:latest

      - name: Build and push nginx
        uses: docker/build-push-action@v6
        with:
          context: ./infra/nginx
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/foodgram-nginx:latest

  deploy:
    needs: build-and-push
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Copy production files to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.SSH_HOST }}
          username: ${{ secrets.SSH_USER }}
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          source: "infra/"
          target: "/root/foodgram"
          overwrite: true

      - name: Deploy on server
        uses: appleboy/ssh-action@v1.2.0
        with:
          host: ${{ secrets.SSH_HOST }}
          username: root
          password: ${{ secrets.SSH_PASSWORD }}
          port: 22
          script: |
            set -e
            cd /root/foodgram
            
            echo "=== Проверка файлов на сервере ==="
            ls -la
            ls -la infra/ || echo "Папка infra не найдена"
            echo "✅ Файлы конфигурации на месте"

            echo "=== Проверка .env файла ==="
            if [ -f ".env" ]; then
              echo "✅ .env файл найден"
            else
              echo "❌ .env файл не найден! Создайте его вручную на сервере"
              exit 1
            fi
            
            echo "=== Остановка старых контейнеров ==="
            docker compose -f docker-compose.prod.yml down || true
            echo "✅ Старые контейнеры остановлены"
            
            echo "=== Загрузка свежих образов из Docker Hub ==="
            docker compose -f docker-compose.prod.yml pull
            echo "✅ Свежие образы загружены"
            
            echo "=== Запуск новых контейнеров ==="
            docker compose -f docker-compose.prod.yml up -d
            echo "✅ Контейнеры запущены"

            echo "=== Ожидание запуска сервисов (40 сек) ==="
            sleep 40
            
            echo "=== Проверка здоровья контейнеров ==="
            docker compose -f docker-compose.prod.yml ps
            
            echo "=== Проверка работы приложения ==="
            if curl -f http://localhost/ > /dev/null 2>&1; then
              echo "✅ Главная страница доступна"
            else
              echo "⚠️ Приложение еще не готово (проверьте логи контейнеров)"
            fi
            
            echo "🎉 Деплой завершен! Сайт: http://food-gram.hopto.org"

      - name: Telegram notify
        uses: appleboy/telegram-action@master
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: |
            🎉 Foodgram: полный деплой завершен!
            
            📊 Статус: Успешно
            🌐 Сайт: http://food-gram.hopto.org
            📝 Репозиторий: ${{ github.repository }}
            🎯 Ветка: ${{ github.ref_name }}
            👤 Автор: ${{ github.actor }}
            
            🚀 Готов к использованию!